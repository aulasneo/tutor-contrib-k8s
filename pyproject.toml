[project]
name = "tutor-contrib-k8s"
description = "Helper plugin for Kubernetes deployments of Open edX"
authors = [
  { name = "Aulasneo", email = "andres@aulasneo.com" },
]
license = { text = "AGPLv3" }
license-files = []
readme = { file = "README.rst", content-type = "text/x-rst" }
requires-python = ">=3.10"
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: GNU Affero General Public License v3",
  "Operating System :: OS Independent",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
]
dependencies = [
  "tutor>=22.0.2,<23.0.0",
]
dynamic = ["version"]

[project.optional-dependencies]
dev = [
  "black",
  "build",
  "isort",
  "jinja2",
  "mypy",
  "pkginfo>=1.12.0",
  "pylint",
  "PyYAML",
  "twine>=7.0.0",
]

[project.urls]
Code = "https://github.com/aulasneo/tutor-contrib-k8s"
Issues = "https://github.com/aulasneo/tutor-contrib-k8s/issues"

[project.entry-points."tutor.plugin.v1"]
k8s = "tutork8s.plugin"

[build-system]
requires = ["hatchling>=1.24.0"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "tutork8s/__about__.py"

[tool.hatch.build.targets.wheel]
packages = ["tutork8s"]

[tool.hatch.build.targets.sdist]
strict-naming = false
include = ["/tutork8s", "/docs", "/README.rst", "/LICENSE.txt", "/CHANGELOG.md"]
exclude = ["tests*"]
